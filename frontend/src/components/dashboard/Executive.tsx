// import { useExecutiveData } from "../../hooks/useExecutiveData";

function Executive() {
  // const { data, isLoading, isError, error } = useExecutiveData()

  return (
    <div className="backdrop-blur-xl rounded-xl bg-linear-120 from-primary3-3 to-primary1-2 to-50%
                    w-[1300px] h-[550px] -mb-[50px] opacity-20">
    </div>
  );
}

export default Executive;
